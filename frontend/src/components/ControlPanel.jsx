import React, { useState, useRef, useEffect, useCallback } from 'react';

function ControlPanel({ onExecute, isExecuting }) {
    return (
        <div className="editor-header">
            <span>JavaScript</span>
           
        </div>
    );
}

export default ControlPanel;